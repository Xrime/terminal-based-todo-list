requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
name = "xrime-weather-cli"
version = "0.1.0"
description = "A simple terminal-based weather checker"
dependencies = [
    "requests",
    "python-dotenv",
]
weather-app = "main:main"
package-dir = {"" = "src"}
py-modules = ["main"]