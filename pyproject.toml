[build-system]
requires =["uv_build >=0.11.6, <0.12.0"]
build-backend = "uv_build"

[project]
name = "resolution_week1_Aint"
version ="1.0"
dependencies =[]

[project.scripts]
todo = "resolution_week1_Aint.main:main"